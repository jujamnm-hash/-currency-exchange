# GitHub Pages ناگونجێت لەگەڵ نیشانە — پێویستی بە PostgreSQL و API سێرڤەر هەیە.
# دامەزراندن تەنها لەسەر Vercel.
name: GitHub Pages (disabled)

on:
  workflow_dispatch:

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use Vercel deploy instead — this app requires Postgres + server APIs."
